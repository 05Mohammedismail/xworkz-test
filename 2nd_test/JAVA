JAVA :

 1) error: incompatible types: int cannot be converted to boolean
                if((boolean)x==true)
				
2)  10 ,110
				
				
3)  error: incompatible types: int cannot be converted to boolean
                if (x)				
				
4) 1.Because we can call the members without creating instances of class
   2.It makes program memory efficient
   3.And we can access Static variable inside static method
   
5)class Array{
	public static void main(String[] args){
		
		int[] values = {10 , 45 , 7 , 9 , 2 };
					System.out.println("The reverse order of array is");

		for (int arrayIndex = values.length-1 ; arrayIndex>-1 ; arrayIndex --  ){
			System.out.println(values[arrayIndex]);
		}
	}
}

6) import java.util.Arrays;
public class Temp {
    public static void main(String[] args) {
        int[] values= {2,5,4,8,2,3,6,2};
        Arrays.sort(values);
        for(int index =0; index< values.length ; index++)
        {
            System.out.println(values[index]);
        }
    }
}


7) Fail

8)"this" is a keyword used to store the address of Local variable inside a instance variable
   defined by : this.variable = variable;
   dot operator is used 
   
  "this()" is used to make chaining
  it is used to write one constructor inside another constructor
  this is helpul to reduce numbers of lines of code
  
9)class Swap{
	public static void main(String[] args ){
		int a =1;
		int b = 2;
		b = a+b;
		a=b-a;   b=b-a ;
		System.out.println("a:"+a+","+"b:"+b);
	}
}

10)
 1)
   public class Pattern {
      public static void main(String[] args) {
        int size = 3; 
        for (int i = 0; i < size; i++) {
            for (int j = 0; j < size; j++) {
                if (j == i || j == size - i - 1) {
                    System.out.print("*");
                } else {
                    System.out.print(" ");
                }
            }
            System.out.println();
        }
    }
} 
2) public class Pattern{
    public static void main(String[] args) {
        int size = 3; 
        for (int i = 0; i < size; i++) {
            for (int j = 0; j < size; j++) {
                if (j == i || j == size - i - 1) {
                    System.out.print("*");
                } else {
                    System.out.print("&");
                }
            }
            System.out.println();
        }
    }
}
 

  3) 
	public class Pattern {
		public static void main(String[] args) {
			int size = 3; 

  
        for (int i = 0; i < size; i++) {
            for (int j = 0; j < size; j++) {
                if (i == 0 || i == size - 1 || j == 0 || j == size - 1) {
                    System.out.print("%");
                } else {
                    System.out.print(" ");
                }
            }
            System.out.println();
        }
    }
}
  


   
